/**
 * Writes package.json's version into src/version.ts.
 *
 * The version has to be a literal in the source: `--compile` produces one file
 * with no package.json next to it, so reading it at runtime is not an option.
 * Runs from `prebuild`, so the literal can never drift from the manifest.
 */
import { readFileSync, writeFileSync } from "node:fs";
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";

const root = join(dirname(fileURLToPath(import.meta.url)), "..");
const { version } = JSON.parse(readFileSync(join(root, "package.json"), "utf-8"));

writeFileSync(
  join(root, "src", "version.ts"),
  `/** Generated by scripts/stamp-version.mjs — edit package.json, not this file. */\nexport const VERSION = ${JSON.stringify(version)};\n`,
);
